
def combine_strings(a, b, c):
    """Correct solution: should return the same results as the model solution.
    """
    result = []
    result.append(a.upper())
    result.append(b.upper())
    result.append(c.upper())
    return result